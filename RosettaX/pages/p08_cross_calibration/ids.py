# -*- coding: utf-8 -*-


class Ids:
    """
    Component identifiers for the cross-calibration page.
    """

    page_prefix = "cross-calibration"

    primary_summary_store = f"{page_prefix}-primary-summary-store"
    secondary_summary_store = f"{page_prefix}-secondary-summary-store"
    result_store = f"{page_prefix}-result-store"

    primary_upload = f"{page_prefix}-primary-upload"
    primary_feedback = f"{page_prefix}-primary-feedback"
    primary_summary = f"{page_prefix}-primary-summary"

    secondary_upload = f"{page_prefix}-secondary-upload"
    secondary_feedback = f"{page_prefix}-secondary-feedback"
    secondary_summary = f"{page_prefix}-secondary-summary"

    build_button = f"{page_prefix}-build-button"
    status = f"{page_prefix}-status"

    graph = f"{page_prefix}-graph"
    table = f"{page_prefix}-table"

    export_name = f"{page_prefix}-export-name"
    export_button = f"{page_prefix}-export-button"
    export_download = f"{page_prefix}-export-download"
