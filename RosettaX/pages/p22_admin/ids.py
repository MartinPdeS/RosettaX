

class Ids:
    """
    Component identifiers for the admin analytics page.
    """

    page_prefix = "admin"

    refresh_interval = f"{page_prefix}-refresh-interval"
    refresh_button = f"{page_prefix}-refresh-button"
    last_updated = f"{page_prefix}-last-updated"
    stat_total_visits = f"{page_prefix}-stat-total-visits"
    stat_unique_visitors = f"{page_prefix}-stat-unique-visitors"
    stat_visits_today = f"{page_prefix}-stat-visits-today"
    stat_unique_visitors_today = f"{page_prefix}-stat-unique-visitors-today"
    stat_apply_clicks = f"{page_prefix}-stat-apply-clicks"
    stat_calibrated_files = f"{page_prefix}-stat-calibrated-files"
    visits_over_time_graph = f"{page_prefix}-visits-over-time-graph"
    page_breakdown_graph = f"{page_prefix}-page-breakdown-graph"
    recent_visits_table = f"{page_prefix}-recent-visits-table"
