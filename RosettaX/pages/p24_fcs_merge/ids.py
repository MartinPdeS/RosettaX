# -*- coding: utf-8 -*-


class Ids:
    """Component identifiers for the FCS merge page."""

    page_prefix = "fcs-merge"

    file_store = f"{page_prefix}-file-store"
    upload = f"{page_prefix}-upload"
    upload_feedback = f"{page_prefix}-upload-feedback"
    export_button = f"{page_prefix}-export-button"
    export_feedback = f"{page_prefix}-export-feedback"
    download = f"{page_prefix}-download"
