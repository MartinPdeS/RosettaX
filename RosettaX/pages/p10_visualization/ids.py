# -*- coding: utf-8 -*-


class Ids:
    """
    Component identifiers for the visualization page.
    """

    page_prefix = "visualization"

    file_store = f"{page_prefix}-file-store"
    upload = f"{page_prefix}-upload"
    upload_feedback = f"{page_prefix}-upload-feedback"
    plot_type = f"{page_prefix}-plot-type"
    x_channel = f"{page_prefix}-x-channel"
    y_channel = f"{page_prefix}-y-channel"
    y_channel_container = f"{page_prefix}-y-channel-container"
    x_log = f"{page_prefix}-x-log"
    y_log = f"{page_prefix}-y-log"
    axis_toggle_container = f"{page_prefix}-axis-toggle-container"
    scatter_controls_container = f"{page_prefix}-scatter-controls-container"
    colormap_log = f"{page_prefix}-colormap-log"
    marker_size_container = f"{page_prefix}-marker-size-container"
    marker_opacity_container = f"{page_prefix}-marker-opacity-container"
    marker_size = f"{page_prefix}-marker-size"
    marker_opacity = f"{page_prefix}-marker-opacity"
    max_events = f"{page_prefix}-max-events"
    metadata = f"{page_prefix}-metadata"
    plot_status = f"{page_prefix}-plot-status"
    graph = f"{page_prefix}-graph"
